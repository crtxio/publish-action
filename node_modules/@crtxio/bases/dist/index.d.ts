import { MultibaseCodec } from './abstract.js';
export { MultibaseCodec } from './abstract.js';

declare type BaseList = Record<string, MultibaseCodec<string>>;
declare class Bases {
    readonly bases: BaseList;
    readonly detector: RegExp;
    constructor(bases?: BaseList);
    encode(bytes: Uint8Array, encoding?: string, prefix?: boolean): string;
    decode(str: string, encoding?: string): Uint8Array;
    ensure(input: string | Uint8Array, encoding?: string): Uint8Array;
    detect(str: string): MultibaseCodec<string>;
    transcode(str: string, to: string, from?: string, prefix?: boolean): string;
}

export { BaseList, Bases };
