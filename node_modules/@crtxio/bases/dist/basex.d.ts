import { MultibaseCodec } from './abstract.js';

declare class BaseX<Prefix extends string> extends MultibaseCodec<Prefix> {
    private _validator;
    private _alphabet;
    private _base;
    private _leader;
    private _basemap;
    private _factor;
    private _ifactor;
    constructor(name: string, prefix: Prefix, alphabet: string);
    get alphabet(): string;
    isValid(str: string): boolean;
    encode(bytes: Uint8Array, prefix?: boolean): string;
    decode(str: string, prefix?: boolean): Uint8Array;
    static from({ name, prefix, alphabet }: BaseXOptions): BaseX<string>;
}
interface BaseXOptions {
    name: string;
    prefix: string;
    alphabet: string;
}

export { BaseX };
