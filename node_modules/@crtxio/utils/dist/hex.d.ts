declare function isValid(str: string): boolean;
declare function isAddr(str: string): boolean;
declare function toBytes(hex: Uint8Array | string): Uint8Array;
declare function fromBytes(bytes: Uint8Array, prefix?: boolean): string;
declare const hex: {
    isValid: typeof isValid;
    isAddr: typeof isAddr;
    toBytes: typeof toBytes;
    fromBytes: typeof fromBytes;
};

export { fromBytes, hex, isAddr, isValid, toBytes };
