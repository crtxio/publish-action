'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function log2Byte(byte) {
  if (!byte) {
    return;
  }
  let bit = 0;
  if (byte >> 4) {
    bit += 4;
    byte >>= 4;
  }
  if (byte >> 2) {
    bit += 2;
    byte >>= 2;
  }
  if (byte >> 1) {
    bit += 1;
  }
  return bit;
}
function randint(range) {
  return Math.floor(Math.random() * range);
}
const calc = {
  log2Byte,
  randint
};

exports.calc = calc;
exports.log2Byte = log2Byte;
exports.randint = randint;
