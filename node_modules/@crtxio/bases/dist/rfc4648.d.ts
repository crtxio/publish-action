import { MultibaseCodec } from './abstract.js';

declare class RFC4648<Prefix extends string> extends MultibaseCodec<Prefix> {
    private _validator;
    private _alphabet;
    private _bits;
    constructor(name: string, prefix: Prefix, alphabet: string, bits: number);
    get alphabet(): string;
    get bits(): number;
    isValid(str: string): boolean;
    encode(bytes: Uint8Array, prefix?: boolean): string;
    decode(str: string, prefix?: boolean): Uint8Array;
    static from({ name, prefix, alphabet, bits, }: RFC4648Options): RFC4648<string>;
}
interface RFC4648Options {
    name: string;
    prefix: string;
    alphabet: string;
    bits: number;
}

export { RFC4648 };
