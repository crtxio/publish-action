declare function encode(num: number): Uint8Array;
declare function decode(bytes: Uint8Array, offset?: number): number;
declare function length(value: number): number;
declare function take(bytes: Uint8Array, offset?: number): [number, number];
declare const varint: {
    take: typeof take;
    encode: typeof encode;
    decode: typeof decode;
    length: typeof length;
};

export { decode, encode, length, take, varint };
