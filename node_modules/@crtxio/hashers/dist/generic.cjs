'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const abstract = require('./abstract.cjs');

class GenericHasher extends abstract.MultihashHasher {
  constructor(name, code, encoder) {
    super(name, code);
    this._encoder = encoder;
  }
  async encode(bytes) {
    return this._encoder(bytes);
  }
  static from({
    name,
    code,
    encoder
  }) {
    return new GenericHasher(name, code, encoder);
  }
}

exports.GenericHasher = GenericHasher;
