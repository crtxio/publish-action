import { MultihashHasher, Encoder } from './abstract.js';

declare class GenericHasher<Code extends number> extends MultihashHasher<Code> {
    protected _encoder: Encoder;
    constructor(name: string, code: Code, encoder: Encoder);
    encode(bytes: Uint8Array): Promise<Uint8Array>;
    static from({ name, code, encoder, }: GenericHasherOptions): GenericHasher<number>;
}
interface GenericHasherOptions {
    name: string;
    code: number;
    encoder: Encoder;
}

export { GenericHasher };
